# Task 1-f — Profile / Settings / NotFound

Agent: subagent 1-f
Task: Build the Profile, Settings and NotFound views for DevFlow AI (frontend only, mock data via Zustand stores).

## Files created / overwritten (all owned by 1-f)
- `src/components/profile/profile-header.tsx` — `ProfileHeader` (named) + `getInitials` helper. Gradient banner, 96px avatar with initials fallback + ring, name/email, role badge, "Member since", Edit/Save/Cancel inline form (name/email/role/bio w/ 280 counter/avatar URL). Validation: name ≥ 2 chars, valid email.
- `src/pages/profile/profile-view.tsx` — `ProfileView` (default). ProfileHeader + 4-card stat grid (Projects owned, Tasks assigned, Completed tasks, Completion rate %) using `AnimatedCounter`; About card with bio + badges; Recent activity card listing the user's 6 latest activities from `useDataStore.activities` with per-action icon + relative time. Match by `user.id` OR `userName === user.name` (mock data is consistent — owner = `user_demo_1` = demoUser.id).
- `src/components/settings/appearance-settings.tsx` — `AppearanceSettings` (named). Light/Dark/System option cards with active ring, check icon, mini preview swatch, mounted-guard to avoid hydration mismatch. Brand accent swatch row ("Brand accent — emerald").
- `src/pages/settings/settings-view.tsx` — `SettingsView` (default). shadcn `Tabs` with desktop vertical nav / mobile horizontal scroll list. Sections: Appearance (renders `AppearanceSettings`), Profile (short card + Edit profile button → `useUIStore.setView('profile')`), Notifications (5 `Switch`es persisted via inlined `useLocalStorage` hook — no new store), Account (read-only email, change-password form with show/hide + validation, danger zone with destructive `ConfirmDialog` → `logout()` + toast). Uses `useToast` for every action.
- `src/pages/not-found-view.tsx` — `NotFoundView` (default). Polished 404: min-h-[70vh], dotted backdrop, Lucide `Compass` in a brand-tinted circle with a pulsing accent dot, big "404" with `text-gradient-brand`, headline "This page drifted off the roadmap.", "Back to dashboard" (→ `setView('dashboard')`) and "Report an issue" (toast "Thanks! Issue noted (demo)"). Framer-motion fade + slight y, reduced-motion respected via global guard.

## Wiring
- `useAuthStore`: read `user`, call `updateProfile`, `logout`.
- `useDataStore`: read `projects`, `tasks`, `activities`; compute stats client-side.
- `useUIStore`: `setView('profile' | 'dashboard')`.
- `useToast`: profile save, password change, delete account, notification toggle, 404 report.
- `next-themes` `useTheme`: actual theme switching in AppearanceSettings.

## Quality checks
- `bun run lint` against my owned files: clean (no errors, no warnings).
- Project-wide lint reports 1 error + 1 warning in 1-a-owned files (`user-menu.tsx`, `auth-mode-tabs.tsx`) — NOT introduced by me; left untouched per ownership rules.
- `tail -80 /home/z/my-project/dev.log`: only successful "Compiled in Xms" + "GET / 200" lines; no compile errors after my edits.

## Notes
- Inline `useLocalStorage` hook in SettingsView per contract ("inline a tiny useLocalStorage hook or skip persistence"). Did NOT create a new zustand slice.
- Did NOT modify any file outside ownership; only consumed stores, common components, layout, shadcn ui, lib.
- All interactive files start with `'use client'`.
- No indigo/blue as primary; brand = emerald everywhere. Sky is only used by existing in-progress task badge (not my code).
- Accessibility: radio group on theme cards, labels for every input, `aria-invalid`/`aria-describedby` on profile/password forms, `aria-current` on desktop tab nav, `aria-label` on icon buttons, semantic `<header>`, `<nav>`, `<main>` (in 404), `<ul>`/`<li>` lists.
- Animations are subtle (≤0.32s) with reduced-motion respected via global guard in `globals.css`.
